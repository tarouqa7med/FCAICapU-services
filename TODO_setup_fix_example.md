# TODO: Fix setup.php database creation

- [ ] Fix `php/setup.php` to avoid requiring `php/config.php` before the database exists.
- [ ] Optionally adjust `php/config.php` to not die with JSON when used by setup.
- [ ] Run `php php/setup.php` again and verify output.

